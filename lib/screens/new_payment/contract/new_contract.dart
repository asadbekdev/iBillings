import 'package:flutter/material.dart';

class NewContractScreen extends StatelessWidget {
  const NewContractScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("New Contract"),
    );
  }
}
